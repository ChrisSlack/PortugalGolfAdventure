# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/golf_trip

# Database Connection Details (automatically set by Replit)
PGHOST=localhost
PGPORT=5432
PGUSER=your_database_user
PGPASSWORD=your_database_password
PGDATABASE=golf_trip

# Development Environment
NODE_ENV=development

# Session Secret (required for user sessions)
SESSION_SECRET=your_session_secret_key_here

# Replit Auth Configuration (automatically set in Replit environment)
REPL_ID=your_repl_id
REPLIT_DOMAINS=your-repl-domain.replit.app
ISSUER_URL=https://replit.com/oidc